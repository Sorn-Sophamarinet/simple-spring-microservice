spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
          native:
            search-locations:
              - classpath:/config-repo/
              - classpath:/config-repo/{application}/

server:
  port: ${SERVER_PORT:8888}

management:
  endpoints:
    web:
      exposure:
        include: health,info
